import { connect } from 'react-redux';
import SettingsScreen from './SettingsScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(SettingsScreen);