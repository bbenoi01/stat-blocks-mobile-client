import { connect } from 'react-redux';
import AddCreatureScreen from './AddCreatureScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(AddCreatureScreen);