import { connect } from 'react-redux';
import CampaignScreen from './CampaignScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(CampaignScreen);