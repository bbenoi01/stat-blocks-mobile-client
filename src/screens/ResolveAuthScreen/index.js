import { connect } from 'react-redux';
import ResolveAuthScreen from './ResolveAuthScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(ResolveAuthScreen);